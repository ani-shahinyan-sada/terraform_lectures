# locals {
#   subnetwork1 = "projects/${var.project_id}/regions/${var.region}/subnetworks/${module.subnet.subnets[var.subnet_locals1].name}"
#   subnetwork2 = "projects/${var.project_id}/regions/${var.region}/subnetworks/${module.subnet.subnets[var.subnet_locals2].name}"
# }